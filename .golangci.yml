version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - misspell
    - noctx
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
          - noctx
